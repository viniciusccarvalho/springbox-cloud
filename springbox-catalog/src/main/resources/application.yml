spring:
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost/ml2
    username: root
    driver-class-name: com.mysql.jdbc.Driver

security:
  basic:
    enabled: false

---

spring:
  profiles: default
  jpa:
    database: MYSQL

---

spring:
  profiles: cloud
  jpa:
    database: MYSQL

security:
  basic:
    enabled: false